<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_login') }}">
            <img id="home-food-icon" src="{{ asset('images/icon/user-management.png') }}" alt="Users">
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent"
                aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">

                {% if is_granted("ROLE_USER") %}
                    <li class="nav-item   {% if app.request.attributes.get('_route') == 'app_teams_groups_management_users' %}nav-active{% endif %}">
                        <a class="nav-link" href="{{ path('app_teams_groups_management_users') }}">Users</a>

                    </li>
                    <li class="nav-item {% if app.request.attributes.get('_route') == 'app_teams_groups_management_groups' %}nav-active{% endif %}"
                        href="{{ path('app_teams_groups_management_groups') }}">
                        <a class="nav-link" href="{{ path('app_teams_groups_management_groups') }}">Groups</a>
                    </li>
                {% endif %}
            </ul>

            <div class="d-flex">
                {% if is_granted('ROLE_USER') %}
                    <div class="btn-group">
                        <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown"
                                data-bs-display="static" aria-expanded="false">
                            {{ app.user.getEmail() }}
                        </button>
                        <ul class="dropdown-menu dropdown-menu-lg-end">
                            <li>
                                <a href="{{ path('app_logout') }}" class="dropdown-item" type="button">Logout
                                </a>
                            </li>
                        </ul>
                    </div>
                {% else %}
                    <div class="btn-group">
                        <button type="button" class="btn btn-secondary dropdown-toggle" data-bs-toggle="dropdown"
                                data-bs-display="static" aria-expanded="false">
                            <i class="fa fa-user" aria-hidden="true"></i>
                        </button>
                        <ul class="dropdown-menu dropdown-menu-lg-end">
                            <li>
                                <a href="{{ path('app_login') }}" class="dropdown-item" type="button">Sign in</a>
                            </li>
                            {% if is_granted("ROLE_USER") %}
                                <li>
                                    <a href="{{ path('app_register') }}" class="dropdown-item"
                                       type="button">Register</a>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</nav>
